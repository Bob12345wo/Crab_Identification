# 开发记录

本文件记录每轮软件修改、验证结果、设备依赖和后续接入事项，便于以后接入 OAK、称重模块、RS485、OneNET 或其他设备。

## 2026-09-11

### 已完成

- 新增 OAK RGB 对齐深度测量，输出支撑平面到背壳的高度。
- 新增深度质量字段、上传持久化队列、断网补传、原子 JSON 写入和待上传文件保护。
- 增加称重有效性、稳定样本数、波动范围、OAK 采集超时、采集重试和自动触发。
- 增加 MQTT 请求 ID 匹配和重复性厚度统计。
- 新增 replay_depth.py，可不连接 OAK 重新计算已有深度样本。
- 标准块多帧结果改用中位数汇总，增加有效帧比例、帧间波动和参考高度误差门槛；失败时仍保留诊断数值。
- 优化识别采集链路：RGB 与 NN 统一同步读取，严格校验 YOLO 输出，按可见关键点判断框外异常，并把深度质量纳入选帧排序。
- 增加识别参数和标定输入校验，候选帧优先选择完整且深度质量合格的结果，并保存每帧排序依据。
- 增加 NN 输出层、模型文件、标定矩阵和图片写入检查，避免硬件或存储异常产生伪成功结果。

### 验证

- 12 项算法和主流程测试通过。
- Python 语法检查通过。
- 已验证合成平面、倾斜平面、深度缺失、离群点、无效姿态和厚度失败场景。
- 已增加标准块参考误差失败和单个有效异常帧的回归测试。
- 已增加 YOLO 张量布局、非有限值和关键点置信度质量测试。
- 未完成真实 OAK、称重、RS485 和 OneNET 网络验证。

### 后续设备接入检查

- OAK：确认 RGB、左右双目、DepthAI 版本、深度单位和时间同步。
- 称重：确认寄存器定义、稳定/有效/超载位、精度寄存器和零点行为。
- RS485：确认帧格式、单位、CRC、超时和断开恢复。
- OneNET：确认物模型字段、MQTT 回复 ID、文件 fid 和重复上传行为。
- 部署：确认 systemd 用户、工作目录、串口权限、日志轮换、磁盘空间和重启恢复。

### 暂不能由软件替代的验证

- 厚度绝对误差、位置误差和反光表面有效率。
- 螃蟹悬空、腿支撑和摆放姿态影响。
- 相机掉线、USB 供电和 24 小时连续运行稳定性。

## 2026-09-20

### 本轮识别流程优化

- RGB 图像与 NN 推理结果统一通过 DepthAI Sync 读取，降低图像和推理错帧风险。
- YOLO Pose 解码支持常见张量布局，并检查输出层、形状和非有限值。
- 识别质量检查增加 bbox 边缘、可见关键点框外数量和关键点置信度判断。
- 候选帧排序加入深度有效率、支撑平面误差和 RGB/深度同步时间差。
- 增加模型文件、标定矩阵、参数范围和图片写入检查。
- 修正主程序默认 blob 路径，自动兼容仓库 `oak_export/` 和板端项目根目录。
- 标准块汇总使用有效帧中位数，并对帧间波动和已知高度误差设置质量门槛。

### 本轮验证

- 自动化测试从 14 项增加到 22 项，全部通过。
- 覆盖 YOLO 张量布局、非有限检测、关键点置信度、NN 输出形状、标准块参考误差和异常帧汇总。
- 当前仍未进行真实螃蟹、真实 OAK 连接、称重模块、RS485 和 OneNET 网络验证。

### 本轮厚度校准优化

- 新增 `thickness_calibration.py` 和 `calibrate_thickness.py`，支持从多个标准块结果拟合有边界的线性系统误差校准。
- 新增 `depth_thickness_calibration.json`，当前使用同一台 OAK 的 20 mm、30 mm 标准块结果拟合；有效原始范围为 18.5625--26.0770 mm，默认禁止外推。
- 测量结果同时保留 `raw_thickness_mm`、`calibrated_thickness_mm` 和 `reported_thickness_mm`，并保留校准前的 `raw_ok` 与质量原因，避免把系统偏差误认为深度质量通过。
- 10 mm 样本低于当前深度分辨率时明确报告 `thickness_below_depth_resolution`；不再把该情况误解为普通参考误差。
- `test_depth_block.py`、`replay_depth.py`、实时 OAK 流程和 OneNET 上报均支持 `--thickness-calibration`；OneNET 使用 `reported_thickness_mm`。

### 本轮真实数据验证

- 20 mm 标准块：原始中位数 18.5625 mm，9/10 帧有效，校准后约 20.0000 mm，离线结果通过。
- 30 mm 标准块：原始中位数 26.0770 mm，10/10 帧有效；原始结果因系统偏差触发 `reference_error_too_high`，校准后约 30.0000 mm，离线结果通过。
- 10 mm 标准块：当前样本无法稳定区分背壳与支撑平面，作为分辨率诊断保留，不参与本次校准。
- 自动化测试扩展到 30 项，覆盖线性校准、范围保护、原始诊断保留、低分辨率状态和 OneNET 最终值选择。
- 尚未完成真实螃蟹厚度、换设备/换安装位置后的重新校准、50 mm 标准块和 OneNET 实机验证。只要更换相机、安装位置、分辨率、Depth 参数、ROI 或测量平面，就必须重新采集标准块并生成校准配置。
